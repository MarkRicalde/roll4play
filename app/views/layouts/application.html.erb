<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Roll4play" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50 font-sans">
    <!-- Navbar -->
    <header class="bg-blue-600 text-white shadow-md">
      <div class="max-w-7xl mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
          <div class="text-2xl font-semibold">Roll4Play</div>
          <nav class="space-x-6">
            <%= link_to "Home", root_path, class: "hover:text-blue-300" %>
            <%= link_to "Campaigns", campaigns_path, class: "hover:text-blue-300" %>
            <%= link_to "Players", players_path, class: "hover:text-blue-300" %>
            <%= link_to "Sessions", sessions_path, class: "hover:text-blue-300" %>
            <%= link_to "About", about_path, class: "hover:text-blue-300" %>
            <%= link_to "Contact", contact_path, class: "hover:text-blue-300" %>
          </nav>
        </div>
      </div>
    </header>

    <main>
      <%= yield %>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-white py-6">
      <div class="max-w-7xl mx-auto text-center">
        <p>&copy; 2025 Roll4Play. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
